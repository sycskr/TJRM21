
1.git clone
2.拷贝到该文件夹下
3.

git add .
git commit -m "Updated the attributes"
git push origin master

#LFS
git lfs install

git lfs track "*.exe"



git add . 

git lfs ls-files

git commit -m "Add the large file"

git push origin master
